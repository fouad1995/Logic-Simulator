set(srcs ${CMAKE_SOURCE_DIR}/src)
set(core_srcs ${srcs}/core/Gate.cpp ${srcs}/core/Node.cpp)

set(includes ${CMAKE_SOURCE_DIR}/include)
set(core_includes ${includes}/core/Gate.h ${includes}/core/Node.h)

add_executable(TestAnd  
               ${core_srcs}
               ${srcs}/and/And_Gate.cpp
               TestAndGate.cpp
               )

target_include_directories(TestAnd 
	                       PRIVATE
						   ${core_includes}
                           ${includes}/and/AND_gate.h
                           )

target_link_libraries(TestAnd ${SYSLIBS} GTest::GTest GTest::Main)

#add_test(NAME AndGate COMMAND TestAnd)
#add_test(AndGate TestAnd)